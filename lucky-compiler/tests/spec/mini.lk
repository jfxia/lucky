project MiniTest

agent Helper
    model DeepSeek

task Hello
    input
        name: String
    output
        greeting: String
    steps
        let g = "Hello"
        return g

goal SayHi
    success
        done
    workflow MainWorkflow

workflow MainWorkflow
    Hello(name = "World")
